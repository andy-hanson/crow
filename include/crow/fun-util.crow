no-std

# Calls the function and returns its return value.
+subscript[r, p] r(function fun* r(p), params p) builtin, bare

# Calls the function and returns its return value.
+subscript[r, p] r(function fun r(p), params p) builtin

# Calls the function and returns its return value.
+subscript[r, p] r(function act r(p), params p) builtin
