###
Functions for use in tests.
###

no-std
import
	./compare: equal
	./json: json, to
	./misc: new, to, void
	./string: interpolate
export
	./private/exception-low-level: test-failure

###
Asserts the the values are equal.
###
+is[t] void(a t, b t) t equal, (json, t) to
	assert a == b : "Expected {a to} is {b to}" test-failure

###
Use this like so:
try
	<< some code >>
	should-throw
catch expected-exception x
	<< assertions, or just '()' >>
###
+should-throw void()
	throw "Should have thrown a different exception before getting here" test-failure
