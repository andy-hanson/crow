no-std
import
	../io/duration: duration, nsec
	../col/fix-arr: fix-arr, new
	../ptr: by-val
	./bootstrap: arr, nat64, str

perf-time record
	by-val
	# TODO:PRIVATE
	ns nat64

perf-ctx record mut
	measure-names mut str[]
	measure-values mut fix-arr measure-value

perf-ctx by-val perf-ctx() noctx
	[], new

measure-value record
	by-val
	count nat64
	# TODO:PRIVATE
	total-ns nat64

total-duration duration(a measure-value)
	a.total-ns nsec
