no-std
import
	./col/array: array
	./col/list: to
	./hash: hasher
	./misc: void
	./number: nat64
	./string: hash-mix, join, string, to
export
	./private/symbol-low-level: ==, <=>, symbol, to

+hash-mix void(hasher hasher, a symbol)
	hasher hash-mix a.to::string

+to symbol(a symbol) bare
	a
+to symbol(a nat64)
	a.to::string.to

+interpolate symbol(...parts string array)
	("" join parts.to) to
