import
	crow/io/print: out, print

main fut nat(_ str[]) summon
	a fut str = "crow",
	b fut str = a then x =>
		out print x
		x.reverse,
	c fut nat = b then y =>
		out print y
		()
	out print "this happens first"
	c
