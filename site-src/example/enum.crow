import
	crow/io/print: out, print

main nat^(_ string[]) summon
	out print "{up opposite}"
	out print "{up == up}"
	out print "{up == down}"
	()

direction enum
	up
	down

opposite direction(a direction)
	match a
	as up
		down
	as down
		up
