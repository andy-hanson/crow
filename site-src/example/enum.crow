import
	crow/io/print: out, print

main nat^(_ string[]) summon
	out print up.opposite.enum-to-string
	out print (up == up).to-string
	out print (up == down).to-string
	()

direction enum
	up
	down

opposite direction(a direction)
	match a
	as up
		down
	as down
		up
