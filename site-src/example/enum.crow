import
	crow/io/print: out, print

main fut nat(_ str[]) summon
	out print up.opposite.enum-to-str
	out print (up == up).to-str
	out print (up == down).to-str
	()

direction enum
	up
	down

opposite direction(a direction)
	match a
	as up
		down
	as down
		up
