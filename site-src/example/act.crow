import
	crow/cell
	crow/io/print: out, print

main fut nat(_ array str) summon
	xs list str = "crow", "raven"
	count cell nat = 0,
	ys list str = for x : xs
		"{count get-and-incr}: {x}"
	out print ("; " join ys)
	()

get-and-incr nat(a cell nat)
	res = *a
	*a := *a + 1
	res
