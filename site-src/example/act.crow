import
	crow/cell
	crow/io/print: out, print
	crow/str-util: join

main fut nat(_ str[]) summon
	xs str[] = "crow", "raven"
	count cell nat = 0,
	ys str[] = for x : xs
		"{count get-and-incr}: {x}"
	out print ("; " join ys)
	()

get-and-incr nat(a cell nat)
	res = *a
	*a := *a + 1
	res
