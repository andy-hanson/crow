import
	crow/io/print: out, print

main fut nat(_ arr str) summon
	x sym = "flabbergast"
	out print x.to-str
	out print (x == "glabberfast").to-str
	out print (x == ("flabber" ~~ "gast").str-to-sym).to-str
	()
