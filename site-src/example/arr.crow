import
	crow/io/print: out, print

main fut nat(_ array str) summon
	xs list str = "swan", "crow"
	out print xs[1]
	ys = xs ~ "raven" ~ "nightingale"
	out print (", " join ys)
	out print (", " join ys[1 .. 3])
	()
