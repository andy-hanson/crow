import
	crow/io/print: out, print

main nat^(_ string[]) summon
	for e : enum-members::((lucky-numbers named-val) array)
		out print "{e.name} = {e.val.to::nat8}"
	0,

lucky-numbers enum(nat8)
	friday = 13
	saturday
	route = 66
