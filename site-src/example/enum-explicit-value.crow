import
	crow/io/print: out, print

main nat^(_ string[]) summon
	for name, value : enum-members::((symbol, lucky-numbers) array)
		out print "{name} = {value.to::nat8}"
	0,

lucky-numbers enum(nat8)
	friday = 13
	saturday
	route = 66
