import
	crow/io/print: out, print
	crow/math/vec: +, new, to-str, vec2-64

main fut nat(_ array str) summon
	nats list nat = 0, 1
	vecs list vec2-64 = (0, 1), (2, 3)
	out print "nats sum: {nats sum}"
	out print "vecs sum: {vecs sum}"
	()

add<a> spec
	new a()
	+ a(a a, b a)

sum<a> a(xs list a) add<a>
	() fold xs, (acc, x) =>
		acc + x
