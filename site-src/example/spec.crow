import
	crow/io/print: out, print
	crow/math/vec: +, new, to, vec2-64

main nat^(_ string[]) summon
	nats nat[] = 0, 1
	vecs vec2-64[] = (0, 1), (2, 3)
	out print "nats sum: {nats sum}"
	out print "vecs sum: {vecs sum}"
	()

add[t] spec
	new t()
	+ t(a t, b t)

sum[t] t(xs t[]) t add
	acc mut t = ()
	for x : xs
		acc +:= x
	acc
