import
	crow/io/print: out, print

main nat^(_ string[]) summon
	xs string mut[] = "crow", "raven", "swan"
	if popped ?= xs pop
		out print "popped {popped}"
	xs ~= "crow"
	xs[1] := "crow"
	a = xs move-to-list
	out print "{xs is-empty}"
	out print (" " join a)
	()
