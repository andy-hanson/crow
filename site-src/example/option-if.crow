import
	crow/io/print: out, print

main nat^(_ string[]) summon
	out print "salsa".pair
	out print "pickles".pair
	()

pair string(a string)
	if b ?= a pairing
		"{a} and {b}"
	else
		"{a} alone"

pairing string?(a string)
	if a == "salsa"
		"chips",
