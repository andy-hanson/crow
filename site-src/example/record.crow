import
	crow/io/print: out, print

main nat^(_ string[]) summon
	a bird = "crow", "cap"
	b bird = "seagull", "stetson"
	out print "{a} ✕ {b} = {a mix b}"
	()

bird record
	species string
	hat string

mix bird(a bird, b bird)
	a.species, b.hat

to-string string(a bird)
	"{a species} in a {a hat}"
