import
	crow/io/print: out, print

main void() summon
	for _ : (()::void, ())::(void array)
		out print "array void"
	for i, _ : (()::void, ())::(void array) with-index
		out print "array void {i}"
	for _ : ((), ())::void[]
		out print "list void"
	for i, _ : ((), ())::void[] with-index
		out print "list void {i}"
