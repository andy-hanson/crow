file-ast(
	imports: ports(range(0, 83), [import-ast("/crow/log", some([get-log-handler, info, log, log-handler, warn, with-log-handler]))])
	specs: []
	aliases: []
	structs: []
	funs: [
		fun-decl(
			public: true
			sig: sig-ast(
				range(83, 104)
				main
				inststruct(range(88, 95), name-range(88, fut), [inststruct(range(92, 95), name-range(92, nat))])
				[param(range(96, 103), none, suffix(inststruct(range(98, 101), name-range(98, str)), arr))])
			summon: true
			body: seq-ast(
				call(infix, name-range(118, log), [], [info, literal("Logged once")])
				let(
					name-range(137, lh)
					get-log-handler
					let(
						name-range(159, doubler)
						call(
							prefix
							name-range(169, as)
							[inststruct(range(172, 183), name-range(172, log-handler))]
							[
								lambda(
									[name-range(186, logged)]
									seq-ast(
										call(subscript, name-range(200, subscript), [], [lh, logged])
										call(subscript, name-range(213, subscript), [], [lh, logged])))])
						seq-ast(
							seq-ast(
								call(
									infix
									name-range(231, with-log-handler)
									[]
									[
										doubler
										lambda([], call(infix, name-range(261, log), [], [warn, literal("Logged twice")]))])
								call(
									infix
									name-range(298, with-log-handler)
									[]
									[
										paren(lambda([name-range(282, ignore)], void))
										lambda([], call(infix, name-range(328, log), [], [warn, literal("Logged never")]))]))
							call(infix, name-range(350, resolved), [], [literal(nat(0, false))]))))))])
