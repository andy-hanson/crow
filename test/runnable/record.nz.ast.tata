file-ast(
	imports: [import-ast(0, "/io")]
	exports: []
	specs: []
	aliases: []
	structs: [
		struct(
			range(11, 43)
			true
			[]
			none
			record(
				none
				[
					field(range(29, 34), false, name-range(range(29, 30), x), inststruct(range(31, 34), name-range(range(31, 34), nat)))
					field(range(36, 41), false, name-range(range(36, 37), y), inststruct(range(38, 41), name-range(range(38, 41), nat)))]))]
	funs: [
		fun-decl(
			public?: true
			sig: sig-ast(
				range(43, 71)
				name-range(range(43, 47), main)
				inststruct(range(48, 57), name-range(range(48, 51), fut), [inststruct(range(52, 57), name-range(range(52, 57), int32))])
				[
					param(
						range(58, 70)
						name-range(range(58, 62), args)
						inststruct(
							range(63, 70)
							name-range(range(63, 66), arr)
							[inststruct(range(67, 70), name-range(range(67, 70), str))]))])
			summon: true
			body: let(
				name-range(range(80, 81), m)
				new-record(
					some(inststruct(range(88, 97), name-range(range(88, 97), my-record)))
					[literal(numeric, "1"), literal(numeric, "2")])
				seq-ast(
					seq-ast(
						seq-ast(
							seq-ast(
								call(
									prefix
									name-range(range(105, 111), assert)
									[]
									[
										call(
											prefix
											name-range(range(117, 119), ==)
											[]
											[call(dot, name-range(range(115, 116), x), [], [m]), literal(numeric, "1")])])
								call(
									prefix
									name-range(range(123, 129), assert)
									[]
									[
										call(
											prefix
											name-range(range(135, 137), ==)
											[]
											[call(dot, name-range(range(133, 134), y), [], [m]), literal(numeric, "2")])]))
							call(
								prefix
								name-range(range(141, 151), print-sync)
								[]
								[
									call(
										dot
										name-range(range(157, 163), to-str)
										[]
										[call(dot, name-range(range(155, 156), x), [], [m])])]))
						call(
							prefix
							name-range(range(165, 175), print-sync)
							[]
							[
								call(
									dot
									name-range(range(181, 187), to-str)
									[]
									[call(dot, name-range(range(179, 180), y), [], [m])])]))
					call(prefix, name-range(range(189, 197), resolved), [], [literal(numeric, "0")]))))])
