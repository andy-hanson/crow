main void()
	()

loop-it[t1, b1] t1() (t1, b1) build2
	throw "TODO"

build2[t2, b2] spec
	build2 t2(a b2)

build2 string(_ nat64)
	""

# Testing that the spec `(t0, b0) build2` does not match; we want `build2 string(_ nat64)
sort[t0, b0] string() (t0, b0) build2
	loop-it
