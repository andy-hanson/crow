file-ast(
	imports: ports(range(0, 60), [import-ast("crow/io/print", some([print])), import-ast("crow/ptr", some([const-ptr, size-of]))])
	specs: []
	aliases: []
	structs: [
		struct(
			range(126, 156)
			""
			public
			[]
			none
			record(
				some(by-val-ref(136, by-val))
				[
					field(range(144, 148), const, x, suffix(inststruct(range(146, 147), name-range(146, t)), const-ptr))
					field(range(150, 155), const, y, inststruct(range(152, 155), name-range(152, nat)))]))]
	funs: [
		fun-decl(
			visibility: public
			sig: sig-ast(
				range(60, 82)
				main
				inststruct(range(65, 72), name-range(65, fut), [inststruct(range(69, 72), name-range(69, nat))])
				[param(range(73, 80), none, suffix(inststruct(range(75, 78), name-range(75, str)), arr))])
			summon: true
			trusted: true
			body: seq-ast(
				call(
					prefix
					name-range(98, print)
					[]
					[interpolated([call(single, name-range(107, size-of), [inststruct(range(116, 117), name-range(116, t))], [])])])
				call(empty-parens, name-range(122, new), [], [])))])
