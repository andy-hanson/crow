import
	crow/io/print: out, print

main nat^(_ string[]) summon
	match (() => 1 - 2).catch :: ((nat64, exception) result)
	as ok _
		unreachable
	as err e
		out print "caught the error: {trusted e.to::string}"
	()
