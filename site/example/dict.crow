import
	col.dict: dict, subscript
	col.mut-dict: mut-dict, remove!, set-subscript, subscript
	io.print: print

main fut nat(_ arr str) summon
	a = dict: [10 -> "ten", 1 -> "one"]
	print: a[1] to-str
	print: a[3] to-str

	m = mut-dict: [1 -> "one", 2 -> "two"]
	print: m remove! 1 to-str
	print: m remove! 1 to-str
	m[3] := "three"
	print: m[3] to-str
	0 resolved
