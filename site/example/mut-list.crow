import
	crow/col/mut-list: ~=, is-empty, move-to-arr!, mut-list
	crow/io/print: print
	crow/str-util: join

main fut nat(_ arr str) summon
	xs = mut-list: ["a", "b", "c"]
	xs ~= "d"
	xs ~= ["e", "f"]
	a = xs move-to-arr!
	print: a join ", "
	print: xs.is-empty to-str
	0 resolved
