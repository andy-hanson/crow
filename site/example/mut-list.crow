import
	col.arr-util: join
	col.mut-list: ~=, empty?, move-to-arr!, mut-list
	io.print: print

main fut nat(_ arr str) summon
	xs = mut-list: ["a", "b", "c"]
	xs ~= "d"
	xs ~= ["e", "f"]
	a = xs move-to-arr!
	print: a join ", "
	print: xs.empty? to-str
	0 resolved
