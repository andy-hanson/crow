import
	crow/col/mut-arr
	crow/io/print: print
	crow/str-util: join

main fut nat(_ str[]) summon
	xs str mut[] = "a", "b", "c"
	xs ~= "d"
	# TODO: should not need type annotation
	xs ~= ("e", "f") :: str[]
	a = xs move-to-arr!
	print: a join ", "
	print: xs.is-empty to-str
	0 resolved
