import
	crow/io/print: print

main fut nat(_ str[]) summon
	print: up.opposite enum-to-str
	print: up == up to-str
	print: up == down to-str
	0 resolved

direction enum
	up
	down

opposite direction(a direction)
	match a
	as up
		down
	as down
		up
